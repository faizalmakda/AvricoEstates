# Production connection details for Avrico Estates.
#
# These are the PUBLIC, safe-to-share values (Project URL + publishable key).
# Supabase publishable keys are designed to live in client-side apps — your
# data is protected by the Row Level Security policies in supabase/schema.sql,
# not by keeping this key hidden. Committing them here lets the site build and
# deploy without any extra "secret" configuration.
#
# NEVER put the Secret key (sb_secret_...) / service_role key in this file.

VITE_SUPABASE_URL=https://xlglpzeaufukpbgfusef.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_fTcvIm8xIwTQPahyAdnG9w_YbY2mKnP
